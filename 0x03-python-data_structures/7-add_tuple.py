#!/usr/bin/python3

def add_tuple(tuple_a=(), tuple_b=()):
    moons = ()
    kools = tuple_a + (0, 0)
    looks = tuple_b + (0, 0)
    moons = kools[0] + looks[0], kools[1] + looks[1]
    return (moons)
