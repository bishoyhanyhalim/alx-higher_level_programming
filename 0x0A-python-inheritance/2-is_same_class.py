#!/usr/bin/python3

"""this is a new class and func"""


def is_same_class(obj, a_class):
    """return the finally result ok

    Args:
        this is an arf
    Returns:
        return the finally result ok

    """

    if type(obj) == a_class:
        return (True)

    return (False)
